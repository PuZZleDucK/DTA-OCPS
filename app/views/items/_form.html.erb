<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :unit_price %>
    <%= f.number_field :unit_price %>
  </div>

  <div class="field">
    <%= f.label :special_qty %>
    <%= f.number_field :special_qty %>
  </div>

  <div class="field">
    <%= f.label :special_price %>
    <%= f.number_field :special_price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
